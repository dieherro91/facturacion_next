import styles from '../../page.module.css';

export default function LoadingCosa2() {
  // You can add any UI inside Loading, including a Skeleton.
  return (
    <div className={styles.center}>
      <span>Cosas . 2 Loading...</span>
    </div>
  );
}
